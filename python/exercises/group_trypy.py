mystring = "Hello there peeoplee"

# print mystring.capitalize() # Capitalizes the first character

# print mystring.upper() # CAPS LOCK

# print mystring.lower() # whispers

# print mystring.count("g") # Counts how many times the parameter is in the string

# print mystring.find("n", 5) # Gives index of the starting index of the first occurence, unless we specify beginning. If it doens't find, returns -1

# print mystring.index("can") # Gives index of the starting index of the first occurence. If it doens't find, returns value error

# print mystring.split("t") # Defaults to spaces and splits the string into a list ex. ['STring', 'MEthods', 'can', 'bE', 'found', 'here!']

# woo = " "
# print woo.join(mystring) # Putting left arguement between every string object in the list

# print mystring.replace("ee", "e", 1) # Replaces first with second, third times

# print "aihd{}hpwehweu{}ewrwe".format(mystring, mystring) # Placing argument into the {} and it will take up :x spaces

mylist = (3, "b", 10000, "boop", "a", "baop")
list2 = [202, "whw"]
# print len(mylist) # The length of the list

# print max(mylist) # For some reason str take higher precidence than int

# print min(mylist

# print mylist.index("b")

# mylist.append(list2) # Doesn't auto return itself, just modifies. If appending a list, puts whole list as one index on the end
# print mylist

# print mylist.pop(2) # Returns value by index. Defaults to last one

# mylist.remove("b") # Doesn't auto return itself, just modifies. Modifies by value. Removes first instance
# print mylist

# mylist.insert(2,"c") # Takes 2 arguments, index and value
# print mylist

# mylist.sort()
# print mylist

# mylist.reverse() # Reverses the order
# print mylist

# mylist.extend(list2) # Combines both lists (combines into big item)
# print mylist

list2 = list(mylist) # Turns tuples into a list. Change the values by converting
print mylist









